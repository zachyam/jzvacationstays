FROM conductoross/conductor:3.15.0

# Copy any custom configuration if needed
# COPY conductor.properties /app/config/

# Expose the port
EXPOSE 8080

# Use the default startup script
ENTRYPOINT ["/app/startup.sh"]